<% content_for :site_page_title, 'Sign in' %>

<% content_for :content do %>

<div class="login">
  <h1 class="logo">Loose Button</h1>
  <h3 class="sub-logo"></h3>

  <%= simple_form_for resource, :as => resource_name,
                      :url => registration_path(resource_name),
                      :defaults => { :input_html => { :class => 'form-control' } } do |f| %>
    <%= devise_error_messages! %>
    <%= f.input :name, placeholder: 'Name', autofocus: true, label: false %>
    <%= f.input :email, placeholder: 'Company Email', label: false %>
    <%= f.input :password, placeholder: 'Password', label: false %>
    <%= f.input :password_confirmation, placeholder: 'Password Confirmation', label: false %>
    <%= f.button :submit, 'Sign Up', class: 'btn btn-default' %>
  <% end %>

  <div class="login-related-links">
    <%= render "devise/shared/links" %>
  </div>
</div>

<% end %>
